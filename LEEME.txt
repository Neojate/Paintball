22.03.2019
- - - - - - - - - -
-Se han parametrizado los botones pero están montados para que solo funcionen sobre métodos estáticos:
	new Button<T>(Rectangle rectangle, Texture texture, String method, Object[] param);
		T = clase donde se situa el método estático al que se quiere llamar tras pulsar el botón.
		rectangle = posición y tamaño del botón. Es heredado del padre.
		texture = textura que utilizará el botón. Es heredado del padre
		method = nombre del método estático al que se va a llamar.
		param: array de parámetros que recibe el method.

-La clase Input se ha actualizado. Ahora las llamadas click del mouse reciben como parámetro el enum de Input:
	(typeButton.LEFT_BUTTON || typeButton.RIGHT_BUTTON)
	if (Input.mouseClickPressed(typeButton.LEFT_BUTTON)) ...

-Se han cambiado los packages. Revisar la situacion de cada uno de ellos.


24.03.2019
- - - - - - - - - -
- Se han acabado de añadir los botones que faltaban.
- Se han añadido los textos correspondientes a idiomas y dificultades dentro de la clase Language.
- Se ha creado un array "rows" para controlar las posiciones de los textos en el eje Y.


25.03.2019
- - - - - - - - - -
-Corregido el bug del método centerTextY de PaintToWinUtils
-Añadidos los primeros botones en la clase TitleScreen